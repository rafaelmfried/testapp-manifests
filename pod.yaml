apiVersion: v1
kind: Pod
metadata:
  name: testapp
  labels:
    app: testapp
spec:
  containers:
    - name: testapp
      image: localhost:30000/testapp:b028a81673c6bc8b2657d04fdb4cc5716e01e5c8
      imagePullPolicy: IfNotPresent
      ports:
        - containerPort: 3000
      resources:
        requests:
          cpu: "100m"
          memory: "128Mi"
        limits:
          cpu: "500m"
          memory: "256Mi"
      envFrom:
        - configMapRef:
            name: testapp-config
