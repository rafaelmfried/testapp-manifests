apiVersion: v1
kind: Pod
metadata:
  name: testapp
  labels:
    app: testapp
spec:
  containers:
    - name: testapp
      image: localhost:30000/testapp:1893296cc8e8a7499d27e190ce96d576b037ce3a
      imagePullPolicy: IfNotPresent
      ports:
        - containerPort: 3000
      resources:
        requests:
          cpu: "100m"
          memory: "128Mi"
        limits:
          cpu: "500m"
          memory: "256Mi"
      envFrom:
        - configMapRef:
            name: testapp-config
